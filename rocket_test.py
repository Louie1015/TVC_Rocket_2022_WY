# this is a rocket flight simulation for model rockets

# imports and libraries
import numpy as np
import matplotlib
import turtle
import time

# params n stuff
TIMER = 0


class Simulation(object):
    def _init_(self):
        self.screen = turtle.screensize()
        self.screen.setup(1280, 900)
        self.marker = turtle.Turtle()
        self.marker = 
def main():
    while(TIMER < 5):
        sim = Simulation()
        time.sleep(1)
        timer += 1
    main()
